# Check-in-ready measured results

Run `python run_study.py export` after training (partial studies are labeled).
It creates a new directory here with executed analysis notebooks, CSV tables,
PNG/SVG figures, protocol and manifests. Commit that specific directory in a
results-review PR. Do not add model checkpoints or substitute demonstration data
for measured results. No scientific plots are fabricated in this repository.
