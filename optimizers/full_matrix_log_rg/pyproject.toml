[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name="full-matrix-log-rg"
version="0.1.0"
requires-python=">=3.10"
dependencies=["torch","pandas","weightwatcher"]

[tool.setuptools.packages.find]
where=["."]
include=["full_matrix_log_rg*"]
